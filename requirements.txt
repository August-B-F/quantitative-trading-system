# Production requirements for the quant trading research pipeline.
# Scoped to what `src/` and `scripts/` (excluding `archive/`) actually
# import. The pre-Phase-3 codebase requirements are in
# archive/old_codebase/requirements_old.txt.

# Core numerics
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0
scikit-learn>=1.4.0
pyarrow>=14.0.0

# Config / env
pyyaml>=6.0.1
python-dotenv>=1.0.0

# HTTP / scraping (data fetchers)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0

# Market data
yfinance>=0.2.36
pandas-datareader>=0.10.0
pandas-market-calendars>=4.4.0
fredapi>=0.5.0
pytrends>=4.9.0

# Sentiment / NLP utilities used by alt-data fetchers
nltk>=3.8.0

# Plotting (analysis / report generation)
matplotlib>=3.8.0
