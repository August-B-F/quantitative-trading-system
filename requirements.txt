# Production requirements for the quant trading research pipeline.
# Pinned to versions installed in the working environment.
# pandas_datareader removed (incompatible with current pandas); FRED is now
# fetched via requests against the JSON API + public CSV endpoint, see
# src/data/fetchers/fred_macro.py. fredapi remains optional and is no
# longer required because src/data/fetchers/fred.py falls through to the
# requests-based path on its own.

# Core numerics
numpy==2.2.6
pandas==3.0.2
scipy==1.14.1
scikit-learn==1.5.1
pyarrow==23.0.1

# Config / env
pyyaml==6.0.1
python-dotenv==1.0.1

# HTTP / scraping (data fetchers, including FRED)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11

# Market data
yfinance==0.2.55
pandas-market-calendars==5.3.2
pytrends==4.9.2

# Sentiment / NLP utilities used by alt-data fetchers
nltk==3.9.1

# Plotting (analysis / report generation)
matplotlib==3.10.0

# Terminal UI (scripts/tui.py) — read-only presentation layer.
# textual-serve is the official supporting package for `textual serve`
# (HTTP/WebSocket transport), pulled in only for the --serve mode.
textual==8.2.3
plotext==5.3.2
rich>=13.7
textual-serve==1.1.3

# Web UI (scripts/web_server.py) — FastAPI backend + static front-end.
# psutil is optional but powers the System page's CPU/mem/disk panels.
fastapi==0.115.0
uvicorn[standard]==0.30.6
psutil==6.0.0
