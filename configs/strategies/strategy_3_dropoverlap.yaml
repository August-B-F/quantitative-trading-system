name: DROPOVERLAP
slot: 3
tier: confident_upgrade
description: >
  The cleanest single-layer upgrade in the entire autonomous suite. Take
  OPTIMIZED as-is and remove VGT and XLK from the stable universe — the
  two ETFs with highest pairwise correlation to the remaining tech names
  (SOXX, QQQ, IGV). Nothing else changes.
type: rules_universe_trim
universe: [SOXX, QQQ, IGV, XLE, GLD, SHY]
cash_proxy: SHY
reference_index: SPY
rebalance: month_end
signal:
  kind: regime_conditional_momentum
  lookback_stable: 63
  lookback_transition: 21
sizing:
  top1_weight: 0.50
  top3_weight: 0.50
  top_k: 3
  top3_weighting: inverse_vol
  vol_proxy: atr_21d
gate:
  kind: sma_distance
  index: SPY
  window: 200
  threshold: -0.04
  action: top1_to_cash
fomc_defer:
  enabled: true
  pre_days: 0
  post_days: 2
  defer_days: 3
classifier:
  model: HistGradientBoostingClassifier
  feature_set: core
  retrain_cadence: quarterly
backtest:
  cagr: 0.2396
  sharpe: 1.58
  max_dd: -0.1284
  source: tests/autonomous/LOG.md::P2.04_dropoverlap and P2b.01_do_top3
confidence: CONFIDENT
notes: >
  Minimum overfit risk among all tested upgrades — a single universe
  edit with a clear mechanism (reduce correlated-tech concentration
  drag). +0.08 Sharpe over OPTIMIZED at nearly identical CAGR and
  MaxDD. If this matches or beats OPT forward but ROBUST_STACK does
  not, the layered ML contribution is not earning its complexity over
  the bare universe trim.
